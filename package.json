{
  "name": "freelance-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Premium portfolio website (Next.js) — static, Vercel-ready.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @portfolio/web",
    "dev:web": "npm run dev --workspace @portfolio/web",
    "build": "npm run build --workspace @portfolio/web",
    "start": "npm run start --workspace @portfolio/web"
  },
  "engines": {
    "node": ">=22"
  }
}
