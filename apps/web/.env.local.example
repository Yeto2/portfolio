# Base URL of the portfolio API.
# Browser-side (contact form) uses NEXT_PUBLIC_API_URL.
NEXT_PUBLIC_API_URL=http://localhost:4000
# Server-side data fetching uses API_URL (falls back to the public one).
API_URL=http://localhost:4000
